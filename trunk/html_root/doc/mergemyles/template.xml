<?xml version="1.0"?>
<pseudocode>
    <call_stack><replace var="s"/></call_stack>
    <program_listing>
	<signature>mergeSort(list, first, last)</signature>
	<line line_number="1">1   if(first &#38;#60; last)</line>
	<line line_number="2">2      mid = (first + last) / 2</line>
	<line line_number="3">3      mergeSort(list, first, mid)</line>
	<line line_number="4">4      mergeSort(list, mid + 1, last)</line>
	<line line_number="5">5      merge(list, first, mid + 1)</line>
    </program_listing>
    <variables>
        <variable>first = <replace var="first" /></variable>
	<variable>last = <replace var="last" /></variable>
        <variable>mid = <replace var="mid" /></variable>
    </variables>
</pseudocode>




